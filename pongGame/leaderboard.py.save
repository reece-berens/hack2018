import json
import requests

def postScore(initials, score):
	postReq = requests.post('localhost:8000/scoreboard/post/' + initials + '/' + str(score))
	
def getScoreboard():
	getReq = requests.get('http://localhost:8000/scoreboard/get/')	#print(getReq.text)
	#print(json.loads(getReq.text))
	scores = json.loads(getReq.text)
	for s in scores:
		inner = json.loads(s)
		for i in inner:
			print(i)
	return scores
	
if __name__ == '__main__':
	board = getScoreboard()
